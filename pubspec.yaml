name: vacunacion_app
description: Campaña de Vacunación Canina y Felina

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.13.0
  firebase_auth: ^5.5.0
  cloud_firestore: ^5.6.0
  firebase_storage: ^12.4.0

  flutter_riverpod: ^2.6.1
  go_router: ^15.1.2

  image_picker: ^1.1.2
  geolocator: ^13.0.2
  permission_handler: ^11.4.0

  fl_chart: ^0.70.2
  intl: ^0.20.2
  uuid: ^4.5.1
  connectivity_plus: ^6.1.4

  shared_preferences: ^2.3.2  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true